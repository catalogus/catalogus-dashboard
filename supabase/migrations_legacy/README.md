These migration files were archived from `supabase/migrations` because they share
duplicate version prefixes (for example `20260111`) that block `supabase db push`
against remote migration history.

They are preserved here for reference and audit trail, but are intentionally
excluded from active migration execution.
